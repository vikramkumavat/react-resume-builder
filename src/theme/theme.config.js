const theme = {
  fonts: {
    sans: "Inter, system-ui, -apple-system, 'Segoe UI', Roboto, Helvetica, Arial",
  },
  defaults: {
    primary: '#0ea5e9',
    text: '#0f172a',
  },
}

export default theme
